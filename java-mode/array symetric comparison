# -*- mode: snippet -*-
# name: array symetric comparison
# --
int n = ${1:array}.length;
for(int i = 0; i<n; i++)\{
    for(int j=i+1; j<n; j++)\{
        ${2:compaison_func}($1[i], $1[j]);
    \}
\}